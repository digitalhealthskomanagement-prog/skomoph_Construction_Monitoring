# Routes

TanStack Router auto-generates `routeTree.gen.ts` from files under `src/routes/`.

- `index.tsx` → `/` (home)
- `login.tsx` → `/login` (shared-password gate)
- `updates.tsx` → `/updates` (report feed)
- `__root.tsx` — root layout & head metadata
