ALTER TABLE public.updates ADD COLUMN IF NOT EXISTS image_urls text[] NOT NULL DEFAULT '{}'::text[];
UPDATE public.updates SET image_urls = ARRAY[image_url] WHERE image_url IS NOT NULL AND cardinality(image_urls) = 0;